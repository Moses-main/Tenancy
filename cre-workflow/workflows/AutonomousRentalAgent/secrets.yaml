secretsNames:
  LLM_API_KEY:
    - OPENAI_API_KEY
    - GROQ_API_KEY
  # Additional secrets can be added:
  # PROPERTY_API_KEY:
  #   - PROPERTY_MANAGEMENT_API_KEY
  # RPC_URL:
  #   - SEPOLIA_RPC_URL
  #   - BASE_SEPOLIA_RPC_URL

# Example usage in workflow.ts:
# const llmKey = runtime.getSecret({ id: "LLM_API_KEY" }).result();
# This will check OPENAI_API_KEY first, then fall back to GROQ_API_KEY
